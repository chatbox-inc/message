machine:
  php:
    version: 7.0.4
  environment:
    DB_CONNECTION: sqlite

#dependencies:
#  override:
#    - composer install --prefer-source --no-interaction --dev
#    - docker-compose up -d

database:
  override:
    - "php artisan migrate -n"
test:
  override:
    - make test
