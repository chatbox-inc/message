machine:
  php:
    version: 7.0.4

#dependencies:
#  override:
#    - composer install --prefer-source --no-interaction --dev
#    - docker-compose up -d

database:
  override:
    - php artisan migrate
test:
  override:
    - make test
